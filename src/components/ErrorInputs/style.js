import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    reportErro: {
      color: '#FF0000',
      fontSize: 14,
      marginStart: 8,
      fontWeight: '700',
    },
});

export default styles;
